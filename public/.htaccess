<IfModule mod_rewrite.c>
    RewriteEngine On

    # Redireciona requisições para o Front Controller
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^ index.php [L]
</IfModule>
